@using Bluent.UI.Diagrams.Tools.Drawings.Basic
@using Bluent.UI.Diagrams.Tools.Drawings.Diagram
@inherits BluentComponentBase

<DrawingCanvas @ref=_canvas @attributes=AdditionalAttributes
               id="@Id"
               class="@GetComponentClass()"
               style="@Style"
               SnapSize="@SnapSize"
               Tool="Tool"
               Selection="Selection"
               AllowDrag="AllowDrag"
               AllowPan="AllowPan"
               AllowScale="AllowScale">

    <defs>
        <pattern id="grid-pattern" patternUnits="userSpaceOnUse" width="@GridSize" height="@GridSize">
            @if (GridPattern is null)
            {
                <path fill="none" stroke="#aaa" stroke-width="0.5" d="M 0 0 h @GridSize M 0 0 h 0 M 0 0 v @GridSize M 0 0 v 0"></path>
            }
            else
            {
                @GridPattern
            }
        </pattern>
    </defs>
    <rect width="100%" height="100%" fill="url(#grid-pattern)"></rect>


</DrawingCanvas>
