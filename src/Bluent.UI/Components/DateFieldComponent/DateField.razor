@namespace Bluent.UI.Components
@inherits BluentFieldComponentBase<TValue>
@typeparam TValue

<MaskedField Mask="@GetMask()"
             StartAddon="StartAddon"
             EndAddon="GetEndAddon()"
             Size="Size"
             Class="@Class"
             Style="@Style"
             @bind-Value="CurrentValueAsString"
             @attributes=GetInputAdditionalAttributes()
             id="@GetInputId()"
             dir="ltr"
             autocomplete="off"
             name="@NameAttributeValue" />
@* <div class="@GetComponentClass()" style="@Style">
    @if (StartAddon != null)
    {
        <div class="start-addon">
            @StartAddon
        </div>
    }

    <input @attributes=GetInputAdditionalAttributes()
           @ref=Element
           @key=_key
           type="text"
           @bind-value="@CurrentValueAsString" @bind-value:event="@BindValueEvent" />

    @if (DisplayCalendar)
    {
        <Popover>
            <Trigger>
                <Button Icon="icon-ic_fluent_calendar_20_regular"
                        ActiveIcon="icon-ic_fluent_calendar_20_filled"
                        Appearance="ButtonAppearance.Subtle"
                        Size="GetButtonSize()"
                        disabled="@IsDisabled"/>
            </Trigger>
            <Surface>
                <Calendar TValue=TValue
                          SelectedDate=Value
                          SelectedDateChanged="OnDatePicked"
                          Culture=Culture
                          Mode="Mode"
                          Min="Min"
                          Max="Max">
                </Calendar>
            </Surface>
        </Popover>
    }

    @if (EndAddon != null)
    {
        <div class="end-addon">
            @EndAddon
        </div>
    }
</div>
 *@